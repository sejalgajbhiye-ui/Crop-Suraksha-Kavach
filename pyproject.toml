[project]
name = "crop-suraksha-kavach"
version = "1.0.0"
description = "AI-powered animal detection and alert system"
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "backend.app:app"

dependencies = [
    "flask",
    "flask-cors",
    "opencv-python",
    "ultralytics",
    "pillow",
]